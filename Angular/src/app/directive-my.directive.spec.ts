import { DirectiveMyDirective } from './directive-my.directive';

describe('DirectiveMyDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveMyDirective();
    expect(directive).toBeTruthy();
  });
});
